# CourseScope Frontend

This folder contains the Next.js UI for CourseScope.

Start the full stack from the repo root:

- Windows: `./run_win.bat`
- Linux/macOS: `./run_linux.sh`

For documentation and version history, see:

- `README.md`
- `CHANGELOG.md`
