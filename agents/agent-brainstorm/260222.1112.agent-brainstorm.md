# 260222.1112 - agent-brainstorm

Cette entree de log pointe vers la spec complete dans `agents/agent-brainstorm/modifications.md`.

Changements couverts (issus de `docs/modifications.txt`):

- `/goals`: refonte timeline horizontale (cards + fleches + capsules J-X), calendrier plus compact, autocomplete Ville/Pays (backend proxy) et option globe.
- `/activities/[id]`: polish header (ID inline + date), deplacement de cartes Charts -> Details, distributions plus propres (ticks 5km), tooltips KPIs secondaires, fusion Splits + Temps intermediaires.
- `/traces/[id]`: lissage 20, pace bins limites a 200% cible, bins pente filtres (<1min) et axe centre sur 0.
- `/settings`: ajout purge goals + traduction FR + repositionnement maintenance.

Notes:

- Le repo a deja un pattern de compute backend puis rendu frontend (ex: `/activity/{id}/pace-vs-grade` + `AllureVsPenteChart`). La spec privilegie cette approche pour les histogrammes lourds.
- Le repo utilise deja R3F (`@react-three/fiber` / `drei`); pour le globe, rester sur cette stack limite les dependances.
