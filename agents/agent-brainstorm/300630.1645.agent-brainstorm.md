# Journal de session — agent-brainstorm

Date : 2026-06-30 16:45
Agent : agent-brainstorm.md
Source : agents/modifications.txt

## Fichiers lus

- `agents/modifications.txt` — demande : « modification de la redondance frontend, comme décrit dans docs\audit_application.md »
- `agents/AGENTS.md` — règles globales, workflow, hiérarchie
- `agents/agent-brainstorm.md` — workflow agent-brainstorm
- `README.md` — architecture projet
- `docs/style-frontend-ui.md` — guide UI normatif
- `docs/audit_application.md` — sections 4, 5, 8, 10, 12
- `docs/documentation_update_runbook.md` — procédure doc
- `frontend/src/lib/` — 7 fichiers source (api.ts, metricsFormat.ts, metricsRegistry.ts, paceUtils.ts, proPaceVsGrade.ts, routes.ts, utils.ts)
- 10 pages/composants frontend — détails dans modifications.md §3.1

## Résumé de l'analyse

La demande utilisateur cible la redondance frontend identifiée dans l'audit d'application. L'analyse a confirmé :

- **12 patterns dupliqués** : 3-4 copies par fonction pour `startOfDay`, `dateAtStart`, `formatDateLabel` ; 2 copies pour `isoDateUtc`, `weekStartUtc`, `shiftRangeStart`, `parseFlexibleSeconds`, `formatPaceInputFromSeconds`, `formatTimeInputFromSeconds`, `rollingMean`, `samplePoints`
- **1 doublon interne** : `isValidNumber` défini 2x dans `activity-beta/utils/`
- **2 définitions dupliquées du type `HistoryRange`**
- **4 fichiers inutilisés** : HeroKpi.tsx, activity-beta/ui/MetricTile.tsx, SidebarStats.tsx, activityStore.ts — tous confirmés 0 import

## Décisions de cadrage

1. **3 nouveaux modules** : `dateUtils.ts` (création), ajout dans `paceUtils.ts` (existant), `chartUtils.ts` (création)
2. **`rollingMean` splitté** en `rollingMean` (number[]) et `rollingMeanPoints` (ChartPoint[]) — signatures de retour incompatibles
3. **`buildPoints` déplacé** même sans duplication — cohérence logique
4. **`HistoryRange` déplacé** dans dateUtils.ts
5. **Pas de tests unitaires ajoutés** — fonctions pures déplacées, couvertes par build
6. **Suppressions groupées** — même chantier, zéro impact

## Ambiguïtés rencontrées

- Le `dateAtStart` de GoalsObjectivesMapLeaflet.tsx n'appelle pas `startOfDay` contrairement aux 3 autres copies. Comportement mathématiquement identique confirmé.
- L'interface `SidebarStats` dans `types/api.ts` (type Pydantic) partage le nom avec le composant supprimé. Distincts, aucun lien.
- Le `MetricTile` de `activity-beta/ui/` est un doublon orphelin vs le `MetricTile` canonique dans `components/metrics/`.

## Fichier produit

- `agents/agent-brainstorm/modifications.md` — cahier des charges complet pour agent-dev (11 sections, 7 étapes d'implémentation)

## Points bloquants

Aucun. Le chantier est prêt pour agent-dev.

Tous les fichiers concernés ont été lus, toutes les duplications vérifiées ligne à ligne, toutes les suppressions confirmées 0 import.
