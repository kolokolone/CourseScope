# Session agent-brainstorm — 2026-06-25 10:05

## Fichiers lus

- `docs/modifications.txt` (133 lignes, 5 demandes)
- `AGENTS.md` → `/agents/AGENTS.md` (règles globales)
- `README.md` (architecture projet)
- `docs/style-frontend-ui.md` (conventions UI)
- `docs/metrics_catalog.md` (catalogue métriques)
- `backend/api/schemas.py` (schémas Pydantic)
- `frontend/src/app/activities/[id]/page.tsx` (page activité réelle)
- `frontend/src/app/activities-beta/[id]/page.tsx` (page beta — délègue à ActivityBetaPage)
- `frontend/src/components/activity-beta/ActivityBetaPage.tsx` (layout page beta)
- `frontend/src/components/activity-beta/ActivityBetaHero.tsx` (header beta)
- `frontend/src/components/activity-beta/ActivityBetaSubNav.tsx` (sub-nav + z-index)
- `frontend/src/components/activity-beta/ZonesCard.tsx` (zones — filtre time_pct > 0 identifié)
- `frontend/src/components/activity-beta/ReliefCard.tsx` (relief existant)
- `frontend/src/components/activity-beta/PaceVsGradeCard.tsx` (allure vs pente)
- `frontend/src/components/activity-beta/ActivityMapCard.tsx` (carte)
- `frontend/src/components/activity-beta/ui/BetaCard.tsx` (wrapper beta)
- `frontend/src/components/activity-beta/ui/MiniMetric.tsx` (tuile métrique)
- `frontend/src/components/layout/AppShell.tsx` (shell global)
- `frontend/src/components/layout/PageContainer.tsx` (container global)
- `frontend/src/components/layout/page-metadata.tsx` (métadonnées de page)
- `frontend/src/components/layout/nav.ts` (navigation globale)
- `frontend/src/hooks/useActivity.ts` (hooks useSeriesData, useRealActivity)

## Résumé de l'analyse

### 5 demandes extraites de modifications.txt

1. **D1 — Bouton /activities/[id] → /activities-beta/[id]** : P2, facile. Ajout d'un bouton ghost/outline discret.

2. **D2 — Rectangle superflu sur page beta** : P0. L'ActivityBetaPage crée son propre conteneur (`min-h-screen bg-slate-50` + `max-w-[1440px]`) alors que l'AppShell fournit déjà un PageContainer. Violation de `docs/style-frontend-ui.md` §8 et §13.2. Correction : wrapper simple `space-y-5`.

3. **D3 — Carte au-dessus de la sub-nav** : P1, trivial. `z-20` → `z-30` dans ActivityBetaSubNav.

4. **D4 — Zones 0% masquées** : P1, trivial. Supprimer `.filter(z => z.time_pct > 0)` dans ZonesCard.tsx.

5. **D5 — Nouvel encadré "Relief et pente"** : P1, complexe. Refonte du ReliefCard existant avec 8 métriques, AreaChart, annotations de pente. Déplacé après Allure vs Pente.

### Décisions clés

- **Remplacer le ReliefCard existant** plutôt que créer un doublon
- **Aucun changement backend** — toutes les données sont déjà exposées
- **Style beta conservé** (classes Tailwind directes, pas les tokens design system)
- **Annotations = extrêmes absolus** (pente max positive, pente min négative)
- **Utiliser BetaCard comme wrapper** pour le nouveau ReliefCard

### Points d'attention

- La sub-nav utilise des marges négatives (`-mx-*`) pour s'étendre jusqu'aux bords. Après suppression du double container, ces marges doivent correspondre au padding du PageContainer (`px-4 sm:px-6 lg:px-8`). C'est le cas → pas d'adaptation nécessaire.
- Zone 6 : incertitude sur le nombre réel de zones générées. Le code doit être adaptatif.
- Annotations Recharts : les badges arrondis avec texte ne sont pas natifs → utiliser ReferenceLine/ReferenceDot + Label custom ou SVG brut.

### Emplacement du livrable

`agents/agent-brainstorm/modifications.md` — 11 sections, prêt pour agent-dev.

### Aucun point bloquant
